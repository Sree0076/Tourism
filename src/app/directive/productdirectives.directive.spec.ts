import { ProductdirectivesDirective } from './productdirectives.directive';

describe('ProductdirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductdirectivesDirective();
    expect(directive).toBeTruthy();
  });
});

